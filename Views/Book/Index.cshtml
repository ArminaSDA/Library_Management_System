@model IEnumerable<Book>

@{
    ViewData["Title"] = "Index";
}

@if (TempData["ResultOk"] != null)
{
        <h1 class="alert-success">@TempData["ResultOk"]</h1>
}

<div class="container shadow p-5">

    <div class="col mb-3">
        <a asp-controller="Book" asp-action="Create" class="btn btn-lg btn-primary"><i class="bi bi-file-plus-fill"></i>Add Book</a>
    </div>
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th scope="col">Title</th>
                <th scope="col">Price</th>
                <th scope="col">PublishedYear</th>
                <th scope="col">DateCreated</th>
                <th scope="col">DateUpdated</th>
   
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                    <tr>
                        <td width="20%">
                        @item.Title
                        </td>
                        <td width="20%">
                        @item.Price
                        </td>
                    <td width="20%">
                        @item.PublishedYear
                    </td>
                    <td width="20%">
                        @item.DateCreated
                    </td>
                    <td width="20%">
                        @item.DateUpdated
                    </td>
                 
                        <td>
                            <div role="group" class="w-60 btn-group">
                                <a asp-controller="Book" asp-action="Edit" asp-route-id="@item.Id" class=" btn btn-sm btn-primary"><i class="bi bi-pencil-square"></i>Edit</a>&nbsp;
                                <a asp-controller="Book" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger"><i class="bi bi-trash-fill"></i>Delete</a>
                            </div>
                        </td>
                    </tr>
            }
        </tbody>
    </table>
</div>

